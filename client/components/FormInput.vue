<template>
  <div class="flex justify-center text-center mt-14">
    <form @submit.prevent="getUser" class="flex items-center max-w-sm mx-auto">
      <label for="simple-search" class="sr-only">Search</label>
      <div class="relative w-full">
        <div
          class="absolute inset-y-0 start-0 flex items-center ps-3 pointer-events-none"
        >
          <svg
            class="w-4 h-4 text-gray-500 dark:text-gray-400"
            aria-hidden="true"
            xmlns="http://www.w3.org/2000/svg"
            fill="none"
            viewBox="0 0 18 20"
          >
            <path
              stroke="currentColor"
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M3 5v10M3 5a2 2 0 1 0 0-4 2 2 0 0 0 0 4Zm0 10a2 2 0 1 0 0 4 2 2 0 0 0 0-4Zm12 0a2 2 0 1 0 0 4 2 2 0 0 0 0-4Zm0 0V6a3 3 0 0 0-3-3H9m1.5-2-2 2 2 2"
            />
          </svg>
        </div>
        <input
          v-model="search"
          type="text"
          id="simple-search"
          class="text-gray-900 text-sm rounded-lg block w-full ps-10 p-2.5 outline-none"
          placeholder="Search user name..."
          required
        />
      </div>
      <button
        type="submit"
        class="text-center p-2.5 w-32 ms-2 text-sm font-medium text-white bg-[#07d884] rounded-lg"
      >
        <span>Search</span>
      </button>
    </form>

    <!-- search user -->
    <div v-for="person in user" :key="person.id">
      <p>{{ person.name }}</p>
    </div>
  </div>
</template>

<script setup>
const { findUserByName } = findUser();

const search = useState("search", () => "");

const users = useState("user", () => {});

const getUser = async () => {
 try {
   const user = await findUserByName(search.value)
   console.log(user);
   
 } catch (error) {
  
 }
  
};
</script>
